steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args:
      - gcloud
      - functions
      - deploy
      - user-function-build
      - --region=europe-west3
      - --source=.
      - --trigger-http
      - --runtime=java17
      - --memory=512MB
      - --timeout=90
      - --min-instances=0
      - --max-instances=1
      - --ingress-settings=all
      - --allow-unauthenticated
      - --service-account=user-function-sa@for-developers-343319.iam.gserviceaccount.com